spring.application.name=finalversion

# Configuración de la conexión a SQL Server LocalDB
spring.r2dbc.url=r2dbc:sqlserver://localhost:1433;database=core
spring.r2dbc.username=aplication-core
spring.r2dbc.password=93;ilp50A5Wd

# Driver de R2DBC para SQL Server (asegúrate de incluirlo en el POM)
spring.r2dbc.pool.enabled=true

# Configuración de JPA (si es necesario)
spring.jpa.hibernate.ddl-auto=create
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# Configuración del servidor
server.port=8080

# Configuración de registro
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO
